# Legacy Root Reports Archive

This directory preserves reports that previously lived directly under
`reports/`. Current production outputs use the structured directories:

- `reports/predictions/`
- `reports/postmatch/`
- `reports/articles/`

Files here are historical evidence only. They are excluded from the default
public-output audit unless `--include-archive` is explicitly passed.
